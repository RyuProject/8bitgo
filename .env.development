# 开发模式（npm run dev）的前端变量。
#
# 为什么需要这个文件：Vite 按 mode 加载 env，**`.env.production` 在 dev 下根本不读**。
# 于是「只写在 .env.production 里」的构建期开关在本地一律是 undefined，
# 表现是「线上好的功能本地说不支持」，而且没有任何报错 —— j2me 就是这么消失的。
#
# 那为什么不写进 .env？因为根目录的 `.env` 在 .gitignore 里（第 23 行，那是放密钥的地方），
# committed 不了，下一个人克隆下来又会踩同一个坑。`.env.development` 没被忽略，可以进 git，
# 和已经存在的 `.env.production` 正好对称。
#
# ⚠️ 这里的每一条都必须和 `.env.production` 里的同名项**保持一致**。
#    只在一边改 = 本地和线上行为不同，这正是这个文件要解决的问题。
#
# 只放「dev 和 prod 应当完全相同、且不是机密」的那几条。域名、API 地址这类
# 本地想指到别处的，写进 .env.local（已被忽略，每台机器自己管）。

# Java 手机游戏（FreeJ2ME）。
#
# 这一条**必须有**，否则整个 java 平台在本地是「暂不支持在线运行」：
# src/emulator/paths.ts 里 J2ME_PATH 是 `asDir(import.meta.env.VITE_J2ME_PATH)`，
# **刻意没有默认值**（和 EJS_PATH / RUFFLE_PATH 不同，那两个有默认值所以零配置可用），
# 空串 → runtimeMeta 里 `available: () => Boolean(J2ME_PATH)` 为 false →
# EXT_RUNTIME_OVERRIDES 里 jar/jad → j2me 那两行被跳过 → 没有可用运行时。
#
# 运行时文件本身不用操心：predev 和 prebuild 都会跑 setup-j2me.mjs --if-missing
# 装到 public/j2me/（那个目录不进 git，见 .gitignore 第 61 行）。
# 也就是说文件一直是装好的，缺的从来只是这个开关。
VITE_J2ME_PATH=/j2me/
