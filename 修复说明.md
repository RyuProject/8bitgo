# 8BitGo bug 排查与修复

对 `src/`、`server/`、`scripts/` 全量审查了一遍，分前端 React、后端 Express/SQL/鉴权、模拟器模块、
多语言/SEO 四条线并行排查，每条结论都在沙箱里连真的 MariaDB + 真的浏览器复现验证过。
下面按严重程度列出**已修复**的问题。文件里都加了注释说明原因，方便你自己复核。

---

## 致命（能被人利用，或能让整站挂掉）

### 1. 页面标题存在反射型 XSS
`server/src/ssr.js`

所有 meta 的内容都做了转义，唯独 `<title>` 是原样拼进去的。而搜索页会把 URL 里的 `?q=`
直接放进标题。于是这样一条链接就能在你的域名下执行任意脚本：

```
/games?q=</title><script>...</script>
```

登录态是 localStorage 里的 30 天 JWT，一条链接就能偷走访客（包括你自己）的 token 并接管账号。
无需登录、无需后台配合。**修复**：`<title>` 走 HTML 转义。

修复前 → `<title>搜索「</title><script>alert(1)</script>」`（脚本执行）
修复后 → `<title>搜索「&lt;/title&gt;&lt;script&gt;alert(1)&lt;/script&gt;」`

### 2. `String.replace` 把 `$&` / `` $` `` / `$'` 当替换模式解析，页面被撕碎
`server/src/ssr.js`

往模板里注入首屏 HTML 和 JSON 数据时用的是字符串形式的 `replace`，替换串里的 `$` 是有特殊含义的。
实测 `/games?q=$` + 反引号 重复四次，页面从 45KB 膨胀到 **548KB**、`<head>` 出现 **289 次**，
入口脚本被浏览器加载几百遍。匿名就能触发的放大型 DoS。

同一个 bug 也会被正常内容触发：注入的 JSON 里带着全站游戏和文章，**任何一篇正文里出现
`` $` `` 或 `$'`（技术文章里的 shell 片段极常见）就会同时污染每一个 SSR 页面**。

**修复**：改用函数形式的替换值，`$` 不再被当模式解析。修复后无论怎么构造，页面稳定在 45KB。

### 3. 鉴权中间件不捕获异常，数据库一抖整站 502
`server/src/auth.js`

`requireUser` / `optionalUser` / `requireAdmin` 都是 async 且里面 `await` 查库，但没有 try/catch。
Express 4 不接管 async 中间件的 rejection，Node 22 会把它当未捕获异常直接结束进程 ——
而这个进程同时扛着 API 和 SSR，等于整站下线。不需要攻击者：MySQL 重启、连接打满、
你那条 SSH 隧道断一下，只要撞上任意一条带 token 的请求就会发生。

**修复**：三个中间件补 try/catch；另外在 `index.js` 加了进程级兜底（记录但不退出）。

### 4. 读临时 jar 的文件流没挂 error，一次 ENOENT 杀掉进程
`server/src/j2me.js`

`createReadStream(p).pipe(res)` 没有 `.on('error')`，而 `pipe` 不转发错误。
`existsSync` 到真正打开文件之间有窗口，文件可能刚好被 release 或 TTL 清扫删掉 →
未捕获异常 → 进程退出。这条路径**不需要任何鉴权**。

### 5. 邮箱验证码可以无限爆破，能登进任意账号
`server/src/routes/auth.js`

发码有 60 秒冷却，但**校验完全没有次数限制**，错了既不计数也不作废。
6 位数字共 100 万种，对着管理员邮箱慢慢试就能进去，完全绕过密码。

**修复**：单个验证码最多试 5 次，用完直接作废。实测第 5 次即返回「错误次数过多，请重新获取」。

---

## 严重

### 6. 没配 `GOOGLE_CLIENT_ID` 时，Google 登录跳过 `aud` 校验
`server/src/routes/auth.js` —— 原来写的是 `if (clientId && info.aud !== clientId)`，
空字符串是 falsy，整段被短路。而 `.env.example` 里默认就是空的。
结果：任何人拿**自己应用**（或别的网站）签发的 Google token 都能登进本站的任意邮箱账号。
**修复**：没配就直接拒绝（503），配了就必须匹配；`email_verified` 字段缺失时也拒绝。

### 7. 验证码 Map 只增不减
同上文件。过期条目永不清理、没有上限，且冷却是按邮箱判定的 —— 换邮箱刷几百万次就能把内存吃光。
配了 SMTP 的话，这还是个可以给任意邮箱无限发信的中继。**修复**：加过期清扫 + 一万条上限。

### 8. 下架的游戏、未发布的草稿对所有人公开
`server/src/routes/games.js`、`posts.js` —— `GET /api/games` 和 `/api/posts` 无条件返回全部行。
`curl 你的域名/api/posts` 就能读到还没发布的稿子全文，「下架」「草稿」两个功能形同虚设。
**修复**：默认只返回上架/已发布的；`?all=1` 才给全量且需要管理员身份，后台自己会带口令。

实测：匿名 90 款 / 管理员 91 款；匿名取下架游戏详情 404，管理员 200。

### 9. `/en?utm_source=x` 被当成中文 404
`src/entry-server.tsx` —— 语言解析没剥查询串，`/en?x=1` 的首段被解析成 `en?x=1`，认不出语言。
服务端吐一个中文 404 页（还带 noindex），客户端却按英文首页 hydrate，两边对不上。
分享链接、广告落地页最容易踩到。**修复后**：`<html lang="en">` + 英文首页标题。

### 10. 每个页面有两份 description，排在前面的永远是中文
`server/src/ssr.js` + `index.html` —— 删除模板默认标签的正则是单行的，而模板里的
`description` / `og:description` 是多行书写的，删不掉。搜索引擎和社交卡片取第一份 ——
**8 种语言的搜索摘要和分享卡片全是中文默认文案**。
**修复**：给模板默认标签加 `data-ssr-default` 标记，按标记删；并把这些标签压成单行（文件里有注释提醒）。

### 11. sitemap 和 robots.txt 全部指向 `127.0.0.1`
`scripts/gen-sitemap.mjs` —— 读 .env 的顺序是 `.env` 优先，而 Vite 是 `.env.local` 优先，正好相反。
结果构建产物里烘的是正式域名，sitemap 里 552 条 URL 和 robots.txt 的 Sitemap 行却是本地地址。
另外 `VITE_API_URL=same-origin` 会让 `fetch('same-origin/api/games')` 抛错被吞掉，
静默退回内置演示数据 —— 数据库里真实上架的游戏一条都进不了 sitemap。
**修复**：顺序对齐 Vite；same-origin 换算成本机后端地址。实测现在读到 91 款、域名正确。

### 12. URL 带 hash 时可能整站白屏
`src/components/layout/Layout.tsx` —— `document.querySelector(hash)` 对非法选择器抛异常，
而项目没有 ErrorBoundary，effect 抛错会让 React 卸载根节点。
Facebook 分享回跳会附加 `#_=_`，旧书签可能是 `#1` —— 用户看到的是纯白页面。
**修复**：改用 `getElementById` 并加 try/catch。

### 13. 数据库为空时点「随机一款」白屏
`src/services/games.ts` —— `getRandomGame` 返回类型写的是 `Game`，实际可能是 `undefined`，
调用方直接取 `.slug`。**修复**：返回类型改对，调用方没有游戏时退到 `/games`。

### 14. NES ROM 加载失败后，全站键盘被永久劫持
`src/emulator/adapters/jsnes.ts` —— jsnes 的构造函数是「先挂 document 级键盘监听、
再 loadROM」，而 loadROM 会对不支持的 mapper 抛错。抛错时拿不到实例，也就调不到 destroy()，
那三个监听永远留在页面上，把方向键和 Z/X/A/S 全部吃掉 —— 搜索框打不出字、页面滚不动，
只能刷新。每失败一次叠加一层。
**修复**：改成先建实例、再单独 `loadROM`，失败时实例在手上，能正常清理干净。

### 15. Java (J2ME) 平台永远无法选择本地 ROM
`src/emulator/registry.ts` —— `isPlayable` 说能玩、`runtimesFor` 也返回 FreeJ2ME，
唯独 `resolveRuntime` 的兜底只认平台自己配的 runtime（java 是 null）→ 返回 undefined。
结果「玩本地 ROM」把 Java 列进受支持平台，点进去却显示「暂不支持在线运行」，
**连选文件的按钮都不渲染**。**修复**：兜底退到「任意支持该平台的可用引擎」，三处规则统一。

---

## 一般

| # | 位置 | 问题 |
|---|---|---|
| 16 | `src/services/seo.ts` | JSON-LD 里的 url / 面包屑全指向中文页，与 canonical 自相矛盾，7 种语言的权重都被导回中文站。已改为跟随当前语言 |
| 17 | `src/pages/LoginPage.tsx` | 全站唯一没调 `useSeo` 的页面 → 输出空 `<title>`、没有 canonical、也没有 noindex |
| 18 | `LoginPage` / `ProfilePage` | 已登录用户打开 `/me`、`/login` 会先闪一次登录弹窗（hydration 首帧恒为未登录）。新增 `useAuthReady()` 等登录态确定后再判断 |
| 19 | `server/src/j2me.js` | 临时 jar 按内容哈希命名，两个人玩同一个 .jar 会共用一个文件，谁先关页面就把另一个人的删了。也是个未鉴权的跨用户删除口子。改为每次上传唯一文件名 |
| 20 | `server/src/content.js` | 写库与查询并发时，旧结果会覆盖新缓存，后台改完前台仍是旧内容顶满 60 秒。加了缓存代数判断 |
| 21 | `server/src/index.js` | 500 直接回显 `err.message`，数据库报错会把表名、列名、索引名泄露出去。改为只进日志 |
| 22 | `server/src/index.js` | CORS 注册在 body 解析之后，413/400 响应没有跨域头，前端只看到 CORS 失败；JSON 解析失败也被一律降级成 500。已调整顺序并区分 4xx |
| 23 | `src/components/home/FeaturedCarousel.tsx` | 标语按过滤后的下标取，只要有一款下架，后面每张卡片都顶着别人的标语 |
| 24 | `src/pages/GameDetailPage.tsx` | `genreMap[id].name` 裸取，数据库里有未知类型 id 时详情页白屏 |
| 25 | `src/emulator/detect.ts` | NDS 文件头判断是 4 字符比 3 字符，恒为 false，从没生效过。改为按 0xC0 处的任天堂 logo 判断（顺带把源码里裸写的控制字符换成转义写法）|
| 26 | `src/emulator/EmulatorPlayer.tsx` | 文件头已认出是当前平台，却又被扩展名白名单拦掉（`zelda.sgb`、无后缀的 NES ROM）|
| 27 | `src/emulator/EmulatorPlayer.tsx` | 运行时报错只改状态不销毁，画面被隐藏了声音还在响 |
| 28 | 三个 adapter | 已销毁运行时的迟到回调会打断当前正在跑的游戏。都加了 destroyed 判断，播放器侧也校验会话 id |
| 29 | `src/emulator/adapters/j2me.ts` | iframe 初始 about:blank 会触发一次 load，导致 jar 还没开始传就报「运行中」|
| 30 | `src/emulator/adapters/j2me.ts` | `pagehide` 在页面进 bfcache 时也触发（手机切 App 再切回来），会把还在玩的 jar 删掉。改为看 `event.persisted` |
| 31 | `src/emulator/adapters/emulatorjs.ts` | `EJS_language` 写死 `zh-CN`，英文站、日文站的玩家点开模拟器菜单全是中文 |
| 32 | `src/lib/emulator.ts` | NES 实际跑 jsnes，键位却按 EmulatorJS 显示 —— **A/B 是反的**；而且键位表在模块求值时就取了文案，英文站永远显示中文「方向键」|
| 33 | `src/emulator/EmulatorPlayer.tsx` + 8 个语言文件 | 界面写着「文件不会上传」，但 J2ME 的本地 .jar 确实会传到你的服务器。J2ME 改用单独一句如实说明（含关闭页面即删）|
| 34 | `server/src/ssr.js` | 不存在的页面返回 HTTP 200（soft 404），爬虫会把垃圾 URL 当正常页收录。现在真的回 404 |
| 35 | `src/pages/GamesPage.tsx` / `services/games.ts` | `?sort=乱填` 时下拉框显示「最热门」，列表却完全没排序 |
| 36 | `src/services/api.ts` | 网关的 502 HTML 页会让 `JSON.parse` 抛错，用户看到「Unexpected token '<'」而不是「请求失败：502」|
| 37 | `src/services/seo.ts` | `og:locale` 输出 `en` / `zh-Hans`，不符合 Open Graph 要求的 `en_US` 格式 |
| 38 | `src/services/seo.ts` | 客户端路由切换后，上一页的 description / canonical 残留在 head 里 |
| 39 | `public/robots.txt` | `Disallow: /admin` 只挡得住中文站，`/en/admin`、`/ja/me` 全漏网 |
| 40 | `scripts/gen-sitemap.mjs` | sitemap 收录了 136 条自己 canonical 掉、并且被自己 robots.txt 禁抓的筛选页 |
| 41 | `server/src/ssr.js` | 重新 `npm run build` 后不重启 node 会白屏（模板缓存里还是旧的资源哈希）。改为按 mtime 自动重读 |
| 42 | `server/src/index.js` | 构建产物缺失时会掉进 SSR 兜底返回 HTML，浏览器只报一句含糊的 MIME 错误。现在直接 404 |

另外还有上一轮的「一切以数据库为准」：内置数据和 localStorage 不再参与、后台顶部数据源徽章、
后台能看到已下架的游戏、`VITE_API_URL=same-origin`、`/admin` 不走 SSR —— 一并在这个包里。

---

## 验证方式

- `npx tsc -b` 0 错误；`npx oxlint` 只剩 16 条风格类 warning（都是 fast-refresh 提示）
- 沙箱里起了真的 MariaDB + 真的 Express，用 curl 逐条复现「修复前」「修复后」
- 用真实 Chromium 跑了后台、首页、`/games#_=_`、`/login`、`/en/games`，**零页面级错误**
- 8 个语言文件 key 仍然完全一致（439 个，无缺无多）
- `npm run dev` 的 `/api` 代理实测通（5173 → 8788），所以 `VITE_API_URL=same-origin` 本地也能用

## 没动的地方

- **`server/.env` 里的 `ADMIN_AUTH_DISABLED=1` 还开着** —— 上线前务必删掉或设为 0，
  否则任何人都能删你的游戏和文章。这条我不替你改，因为你本地开发正靠它。
- `ssl/origin.key`（Cloudflare 源站私钥）和 `_to_delete/`（19.4MB 历史 zip）目前**在 git 跟踪列表里**，
  推 GitHub 前必须先 `git rm --cached` 出去。另外 `.git/index.lock` 卡着，不删掉 git 会一直报错。
- `vite.config.ts` 加了 `/api` 开发代理（上次被打断没说完）。不想要的话删掉 `server.proxy` 那几行即可。

## 覆盖方式

把压缩包里的内容覆盖到项目同名位置，不会动你的 `.env` / `.env.local`。
覆盖后跑一次 `npm run build`。
