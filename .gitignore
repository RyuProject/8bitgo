# dependencies
node_modules

# build
dist
dist-ssr
*.local
_to_delete/
# logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
README.md
# ⚠️ 上面那行 README.md 夹在日志规则里，效果是**整个仓库所有 README.md 都被忽略**。
# 后果已经发生过：deploy/netplay/ 和 deploy/cloudgame/ 两份部署说明从来没进 git，
# 而 server/、worker/、deploy/live/ 那三份是当初 git add -f 硬塞进去的。
# 部署说明是运维步骤，丢了只能靠人回忆，所以这里把 deploy/ 下的放行。
!deploy/**/README.md
# env
.env
.env.*.local

# editor
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
# Flash 在线存档桥是构建期必需的正式产物；生产机没有 FFDec，必须随 Git 发布。
# `*.sw?` 原本为了忽略编辑器交换文件，却也会匹配 `.swf`，所以在这里精确放行。
#
# ⚠️ 每加一代桥都要在这里加一行。漏了不会报错，只是**文件悄悄不进 Git**：
# 本地一切正常（文件就在磁盘上），部署到生产机才发现 404 —— 而在线存档拿不到桥
# 既不报错也不影响游戏启动，只是玩家永远存不上档。
!public/flash-api/armor-games/AGI.swf
!public/flash-api/armor-games/AGI2.swf
# AGI2 的种子模板：它的文档类就叫 KrfAgiBridge，是「名字的载体」，
# 重建 AGI2.swf 时要用（FFDec 不能指定文档类），必须一起进仓库。
!flash-api/armor-games/template-agi2.swf

# 调试用的**真机 ROM** 不进 git。
# `__ejs-mame-test.html` / `__win95-test.html` 这两份复现页面本身要留（它们是排查工具，
# 只有几 KB），但它们跑的 ROM 是商业游戏 —— 曾经把 `__mxsqy.zip`（明星三缺一，5MB）
# 提交进来，于是 https://8bitgo.com/__mxsqy.zip 直接 200，等于对外分发。
# ROM 自己放到本地即可，页面拿不到时会报一句「zip 加载失败」，不影响其它功能。
public/__*.zip
!public/__win95game.zip   # 77 字节的合法 MZ 空壳，不是 ROM

# QEMU WebAssembly 运行时（public/qemu-wasm/，约 140MB）。
# 它只服务 /linux 这一个页面，是纯静态资源，构建期不需要存在。
# 140MB 里光 load-rootfs.data 就 79MB —— 进 git 意味着每次 clone / 每次 git pull
# 都要搬这么多，而它和 ruffle / jsdos / j2me 一样是「上游产物」，
# 按同一套约定处理：加进 .gitignore，由 npm run qemu 在 predev / prebuild 时准备。
# ⚠️ 放行的是本站自己写的那三样：start.js（引导器）、SOURCE.txt（来源 + SHA-256 清单，
#    装的时候要拿它验哈希）、licenses/（GPL / Apache / MIT 的许可证副本，分发义务要求留着）。
public/qemu-wasm/*
!public/qemu-wasm/start.js
!public/qemu-wasm/SOURCE.txt
!public/qemu-wasm/licenses

# 自建的 EmulatorJS（main 分支构建，约 1.6MB）**故意提交进仓库**，别再忽略它。
# 理由：它和 ruffle / jsdos 不一样 —— 那两个 `npm run ruffle|jsdos` 能从 node_modules
# 复制出来，这个得 clone EmulatorJS 仓库现构建，构建机上没人会去做这一步。
# 之前忽略它的后果：线上悄悄退回 CDN 的 4.2.3（不含 dontExtractIfCore），
# 街机的 neogeo.zip BIOS 被解压，FBNeo 报「Neo Geo BIOS 成员缺失」，本地却一切正常。
# 更新它：见 deploy/netplay/README.md 第一节。

# 核心（public/emulatorjs/cores/，34MB）也**故意提交**。试过改从 npm 装
# （@emulatorjs/core-*）：每个核心包都依赖 @emulatorjs/emulatorjs，后者又可选依赖
# @emulatorjs/cores = 官方全部 50 个核心，一句 npm install 拽下几百 MB。
# 升级核心：npm run ejscores（见 scripts/copy-ejs-cores.mjs 头注释）。

# Play! 的官方 Web 运行时（public/play/）也**故意提交**。生产机没有 emscripten，
# 不能指望部署时现场编译；Play.js 和 Play.wasm 又必须同批更新。来源与哈希记录在
# public/play/runtime.json，npm run test:play / prebuild 会一起校验。

# Cloudflare Worker
worker/node_modules
worker/.wrangler

# Ruffle 运行时（npm run ruffle 从 node_modules 复制）
public/ruffle

# js-dos 运行时（npm run jsdos 从 node_modules 复制）
public/jsdos

# FreeJ2ME 运行时（predev / prebuild 从上游稀疏检出）
# 不进 git，但构建前必须自动安装，否则线上 /j2me/run.html 会 404。
public/j2me
.j2me-tmp

# webretro 运行时（npm run webretro 从 GitHub 稀疏检出，约 80MB）
public/webretro

# webretro 安装脚本的临时检出目录（正常会自动清掉，进程被杀时可能残留）
.webretro-tmp

# 构建期间用来把 public/ 下几个超大目录挪出去的暂存区（见 vite.config.ts 的
# skipHugeStatic）。正常构建结束就立刻移回；进程被强杀时可能残留。
.public-staging

# 平台 BIOS（Neo Geo 这类平台必须有，见 public/bios/HOWTO.md）
# 只忽略文件本身，保留说明文档：BIOS 一般是有版权的商业固件，
# 进了仓库历史就很难彻底删干净，公开仓库更等于对外分发。
public/bios/*
!public/bios/HOWTO.md

# 自托管网页游戏的「游戏数据包」不进 git。
# public/web/cs15/packs/ 里的 base.zip.gz 有 550M：远超 GitHub 单文件 100M 上限
# （Git LFS 免费额度 1GB 存储 / 1GB 每月流量，服务器每次 git pull 都要拉一遍，扛不住），
# 而且它是 Valve 的游戏数据 —— 进公开仓库等于对外分发，同上面 BIOS 那条的道理。
# 数据包放对象存储 / 服务器本地；整个 cs15 目录先不进仓库，等数据包落到 R2 再重新划线。
# （helf-life 目前只有一个 index.html，不受影响。）
public/web/cs15
# cs16：源码（index.html + cs16.js）进 git；只有 447M 的完整 CS1.6 资产包不进 git（Valve 游戏数据 + 超 100M 单文件上限）。
# 部署时把 public/web/cs16/packs/ 整体传到服务器本地即可（运行时通过 ?root=../cs15/ 复用 cs15 引擎与 cs16-client wasm）。
public/web/cs16/packs

# Cloudflare 源站证书与私钥（Authenticated Origin Pulls 的客户端证书）
# 2026-09-06 被 GitGuardian 抓到：origin.key 从 8/26 起就在跟踪列表里，
# 随当天的 push 暴露。文件留在磁盘上（nginx 还要用），但永远不进 git。
# 谁拿到这把私钥，就能冒充 Cloudflare 直连源站、绕开 WAF 和防刷。
ssl/
